#**
  Select statement for a relational database data source
*#
{
    "version": "2018-05-29",
    "statements": [
           "UPDATE mysqlauroradb.ParkingSpotDetails SET status = $ctx.args.status where ID=$ctx.args.ID",
        "SELECT * FROM mysqlauroradb.ParkingSpotDetails WHERE ID=$ctx.args.ID"
    ]
}